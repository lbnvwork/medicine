{% extends 'admin/index.html.twig' %}

{% block title %}RiskFactorType{% endblock %}
{% block h1 %}<h1>RiskFactorType</h1>{% endblock %}
{% block right %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ risk_factor_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ risk_factor_type.name }}</td>
            </tr>
            <tr>
                <th>Enabled</th>
                <td>{{ risk_factor_type.enabled ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('risk_factor_type_index') }}">back to list</a>

    <a href="{{ path('risk_factor_type_edit', {'id': risk_factor_type.id}) }}">edit</a>

    {{ include('risk_factor_type/_delete_form.html.twig') }}
{% endblock %}

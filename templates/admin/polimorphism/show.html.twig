{% extends 'admin/index.html.twig' %}

{% block title %}Polimorphism{% endblock %}
{% block h1 %}<h1>Polimorphism</h1>{% endblock %}
{% block right %}

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ polimorphism.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ polimorphism.name }}</td>
        </tr>
        <tr>
            <th>Title</th>
            <td>{{ polimorphism.title }}</td>
        </tr>
        <tr>
            <th>Enabled</th>
            <td>{{ polimorphism.enabled ? 'Yes' : 'No' }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ polimorphism.description }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('polimorphism_index') }}">back to list</a>

    <a href="{{ path('polimorphism_edit', {'id': polimorphism.id}) }}">edit</a>

    {{ include('admin/polimorphism/_delete_form.html.twig') }}
{% endblock %}

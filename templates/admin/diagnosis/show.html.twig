{% extends 'admin/index.html.twig' %}

{% block title %}Diagnosis{% endblock %}
{% block h1 %}<h1>Diagnosis</h1>{% endblock %}
{% block right %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ diagnosi.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ diagnosi.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ diagnosi.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('diagnosis_index') }}">back to list</a>

    <a href="{{ path('diagnosis_edit', {'id': diagnosi.id}) }}">edit</a>

    {{ include('admin/diagnosis/_delete_form.html.twig') }}
{% endblock %}

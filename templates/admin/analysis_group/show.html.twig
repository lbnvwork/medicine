{% extends 'admin/index.html.twig' %}

{% block title %}AnalysisGroup{% endblock %}
{% block h1 %}<h1>AnalysisGroup</h1>{% endblock %}
{% block right %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ analysis_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ analysis_group.name }}</td>
            </tr>
            <tr>
                <th>FullName</th>
                <td>{{ analysis_group.fullName }}</td>
            </tr>
            <tr>
                <th>Enabled</th>
                <td>{{ analysis_group.enabled ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('analysis_group_index') }}">back to list</a>

    <a href="{{ path('analysis_group_edit', {'id': analysis_group.id}) }}">edit</a>

    {{ include(template_path~'_delete_form.html.twig') }}
{% endblock %}

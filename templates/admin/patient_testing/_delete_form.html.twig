<form method="post" action="{{ path('patient_testing_delete', {'id': patient_testing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ patient_testing.id) }}">
    <button class="btn">Delete</button>
</form>

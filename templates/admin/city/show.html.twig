{% extends 'admin/index.html.twig' %}

{% block title %}City{% endblock %}
{% block h1 %}<h1>City</h1>{% endblock %}
{% block right %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ city.name }}</td>
            </tr>
            <tr>
                <th>Enabled</th>
                <td>{{ city.enabled ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('city_index') }}">back to list</a>

    <a href="{{ path('city_edit', {'id': city.id}) }}">edit</a>

    {{ include('admin/city/_delete_form.html.twig') }}
{% endblock %}

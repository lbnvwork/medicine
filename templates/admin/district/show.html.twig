{% extends 'admin/index.html.twig' %}

{% block title %}District{% endblock %}
{% block h1 %}<h1>District</h1>{% endblock %}
{% block right %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ district.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ district.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('district_index') }}">back to list</a>

    <a href="{{ path('district_edit', {'id': district.id}) }}">edit</a>

    {{ include('admin/district/_delete_form.html.twig') }}
{% endblock %}

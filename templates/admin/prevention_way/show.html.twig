{% extends 'admin/index.html.twig' %}

{% block title %}PreventionWay{% endblock %}
{% block h1 %}<h1>PreventionWay</h1>{% endblock %}
{% block right %}
    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ prevention_way.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ prevention_way.name }}</td>
        </tr>
        <tr>
            <th>MinTotalPoints</th>
            <td>{{ prevention_way.minTotalPoints }}</td>
        </tr>
        <tr>
            <th>Enabled</th>
            <td>{{ prevention_way.enabled ? 'Yes' : 'No' }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('prevention_way_index') }}">back to list</a>

    <a href="{{ path('prevention_way_edit', {'id': prevention_way.id}) }}">edit</a>

    {{ include('admin/prevention_way/_delete_form.html.twig') }}
{% endblock %}

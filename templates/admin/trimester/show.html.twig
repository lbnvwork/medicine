{% extends 'admin/index.html.twig' %}

{% block title %}Trimester{% endblock %}
{% block h1 %}<h1>Trimester</h1>{% endblock %}
{% block right %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trimester.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ trimester.name }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ trimester.title }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ trimester.number }}</td>
            </tr>
            <tr>
                <th>Enabled</th>
                <td>{{ trimester.enabled ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('trimester_index') }}">back to list</a>

    <a href="{{ path('trimester_edit', {'id': trimester.id}) }}">edit</a>

    {{ include(template_path~'_delete_form.html.twig') }}
{% endblock %}

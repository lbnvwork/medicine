{% extends 'admin/index.html.twig' %}

{% block title %}Analysis{% endblock %}
{% block h1 %}<h1>Analysis</h1>{% endblock %}
{% block right %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ analysi.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ analysi.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ analysi.description }}</td>
            </tr>
            <tr>
                <th>Enabled</th>
                <td>{{ analysi.enabled ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('analysis_index') }}">back to list</a>

    <a href="{{ path('analysis_edit', {'id': analysi.id}) }}">edit</a>

    {{ include(template_path~'_delete_form.html.twig') }}
{% endblock %}
